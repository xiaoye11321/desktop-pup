## 1. Electron 应用骨架

- [x] 1.1 创建 `package.json`、启动脚本和 Electron 主进程入口。
- [x] 1.2 创建透明、无边框、固定尺寸、始终置顶且不显示在任务栏的宠物窗口。
- [x] 1.3 创建启用 context isolation 的 preload 桥接，并保持渲染进程禁用 Node 集成。

## 2. 桌面游走

- [x] 2.1 在主进程维护宠物窗口的位置、二维速度和固定频率移动循环。
- [x] 2.2 基于当前显示器的 `workArea` 计算宠物窗口允许的坐标范围。
- [x] 2.3 在每次移动中钳制窗口坐标，并在命中每个轴的边界时独立反转对应速度。
- [x] 2.4 向渲染进程发送移动方向与边界反弹事件，用于同步视觉状态。

## 3. 小狗外观与互动

- [x] 3.1 创建透明渲染页面，并以 HTML/CSS 绘制小狗、行走/蠕动动画和方向朝向。
- [x] 3.2 实现点击处理：从游走状态切换到面向用户、吐舌的卖萌姿态。
- [x] 3.3 通过 preload IPC 暂停主进程的移动循环，并在卖萌状态停止行走动画。
- [x] 3.4 实现再次点击：恢复移动循环、行走动画及此前移动方向。

## 4. 验证

- [x] 4.1 安装依赖并启动应用，确认透明窗口、置顶行为和任务栏隐藏符合预期。
- [x] 4.2 手动验证小狗在四条工作区边界均不会越界且会向正确方向折返。
- [x] 4.3 手动验证两次点击分别进入卖萌姿态和恢复游走。
- [x] 4.4 运行 OpenSpec 验证，确认实现与规格、设计和任务一致。
